import SimpleForm from "components/SimpleForm";

const App = (): JSX.Element => (
  <main>
    <h1>Hello, React Form!</h1>
    <section>
      <SimpleForm />
    </section>
  </main>
);

export default App;
